import React from "react";

//& Not found page
const NotFound = () => {
  return (
    <div
      style={{
        background: "#fff",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "#000",
        fontSize: "32px",
        height: "100vh",
      }}
    >
      URL NOT FOUND
    </div>
  );
};

export default NotFound;
